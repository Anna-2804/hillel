"use strict"
let arr = [1, 2, 3, -1, -2, -3];
let emptyArr = [];
function NewFan(numbers) {
    for (let i = 0; i > arr.length; i++) {
        if (numbers > 0) {
            return emptyArr.push(arr)
        }
        return null
    }
}
console.log(emptyArr)